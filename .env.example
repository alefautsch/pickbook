ANTHROPIC_API_KEY=sk-ant-your-key-here
DATABASE_URL=postgresql+psycopg://blackbook:blackbook@localhost:5444/blackbook
SLEEPER_USERNAME=alefautsch
CORS_ORIGINS=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000

# External sync scheduler (§9.1) — not in-process; Railway Cron or local cron hits POST /sync
SYNC_CRON=0 6 * * *
SYNC_ENABLED=true
# Optional gate for POST /admin/recompute-history
# ADMIN_TOKEN=change-me

# Streamlit Pickbook URL for sidebar link (Phase 6)
# NEXT_PUBLIC_PICKBOOK_URL=http://localhost:8501

# Top-N unrostered players scored per league at sync for FA board (§14.2)
# FA_POOL_SIZE=150
